@*@typeparam TEntity*@

<!--@if (EntityList != null || EntityList.Count > 0)
{
    <div class="table-responsive">
        <table class="table table-hover">

            @switch (EntityList[0])
            {
                case ApplicationUser:
                    <thead class="thead-light">
                        <tr>
                            <th>Id</th>
                            <th>Name</th>
                            <th>Username</th>
                            <th>User Role</th>
                            <th></th>
                        </tr>
                    </thead>
                    break;
                case Company company:-->
                    @*<p>Company Search Results</p>*@
                    <!--break;
                case Project project:-->
                    @*<p>Project Search Results</p>*@
                    <!--break;
            }
            <tbody>
                @foreach (var entity in EntityList)
                {
                    @switch (entity)
                    {
                        case ApplicationUser user:
                            <tr>
                                <td>@user.Id</td>
                                <td>
                                    @user.FirstName @user.LastName
                                </td>
                                <td>
                                    @user.UserName
                                </td>
                                @if (user.UserRoles == null)
                                {
                                    <td><em>No Role</em></td>
                                }
                                else
                                {
                                    <td>
                                        @foreach (var val in user.UserRoles)
                                        {
                                            @val.Role.Name<span>&nbsp;&nbsp;</span>
                                        }
                                    </td>
                                }

                                <td @onclick="()=>AddEntityToList(entity)">
                                    <span class="oi oi-plus" aria-hidden="true"> Assign User</span>
                                </td>
                            </tr>
                            break;
                        case Company company:
                            <p>Company Search Results</p>
                            break;
                        case Project project:
                            <p>Project Search Results</p>
                            break;
                        default:-->
                            @*<tr>No type</tr>*@
                            <!--break;
                    }
                }
            </tbody>
        </table>
    </div>
}
else
{
    <dd class="col-sm-10">
        <em>None</em>
    </dd>
}-->

@* switch: file:///C:/Users/Dinuka/Documents/IIT/EAD/Tutorial_2.pdf*@

@code {
    @*//[Parameter]
    //public List<TEntity> AssignedEntityList { get; set; }

    [Parameter]
    public List<TEntity> EntityList { get; set; }

    [Parameter]
    public EventCallback<ApplicationUser> OnEntitiyAssigned { get; set; }

    private void AddEntityToList(TEntity entity)
    {
        //AssignedEntityList.Add(entity);
    }*@
}
